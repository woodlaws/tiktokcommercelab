import type { MetadataRoute } from 'next'; import { pageContent,posts,site } from '@/lib/site-data';
export default function sitemap():MetadataRoute.Sitemap{const staticPaths=['','insights','seminar','live-agency','contact/brand','contact/seller','privacy','terms',...Object.keys(pageContent)];return[...staticPaths.map(path=>({url:`${site.url}/${path}`,lastModified:new Date(),changeFrequency:path==='insights'?'daily' as const:'monthly' as const,priority:path===''?1:.7})),...posts.map(post=>({url:`${site.url}/insights/${post.slug}`,lastModified:new Date(post.date),changeFrequency:'monthly' as const,priority:.8}))]}
