import { posts,site } from '@/lib/site-data';
export function GET(){const items=posts.map(post=>`<item><title><![CDATA[${post.title}]]></title><link>${site.url}/insights/${post.slug}</link><guid>${site.url}/insights/${post.slug}</guid><pubDate>${new Date(post.date).toUTCString()}</pubDate><description><![CDATA[${post.summary}]]></description></item>`).join('');const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>${site.name}</title><link>${site.url}</link><description>${site.description}</description><language>ko-KR</language>${items}</channel></rss>`;return new Response(xml,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}})}
